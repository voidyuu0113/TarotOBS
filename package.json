{
  "name": "tarot-overlay",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:single": "tsc -b && vite build --mode singlefile",
    "serve:single": "node scripts/static-server.mjs dist-single 4174 127.0.0.1",
    "single:obs": "npm run build:single && npm run serve:single",
    "preview": "vite preview"
  },
  "dependencies": {
    "pixi.js": "^8.9.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
